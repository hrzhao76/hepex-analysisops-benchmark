id: t002_hmumu
type: hmumu
mode: call_white

# --- data requirements ---
needs_data: true
release: 2025e-13tev-beta
dataset: data
skim: 2muons
protocol: https
max_files: 3
cache: true
reuse_existing: true

# --- spec bundle files ---
rubric_path: rubric.yaml
eval_ref_path: eval_ref.yaml
white_prompt_path: white_prompt.md
judge_prompt_path: judge_prompt.md

description: "Search for H->mumu decay using VBF selection."
constraints:
  time_limit_sec: 600
  memory_gb: 8
  allow_network: false
